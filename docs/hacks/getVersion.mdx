---
title: 'getVersion'
section: 'Hacks'
description: ''
---

# getVersion

Updates to proxied objects tracked internally in Valtio with a version number. Therefore `getVersion` helper can be used to check if a proxied object has been updated. This is not typically useful or recommended to use in application code.

```js
getVersion(proxyObject)
```
